# Task 3 fix round 1 report

Status: Astra findings reproduced and fixed in the six-file Task 3 scope; commit awaits root index GO.

## Root causes and fixes

1. `resolveAction` discarded every continuation whenever the configured next boundary was named `midnight`, even when an earlier explicit budget ended execution. Continuation invalidation now requires an actual midnight interruption (`reachedBoundary` plus `id === 'midnight'`).
2. `resolveExecutedActionNarrativeContext` cleared `enRouteNpcIds` for same-destination resume but reused the original proposal directive. The en-route sentence is now generated by one helper and replaced with the no-encounter sentence only when this resolution executed no trip to the destination. A real first travel leg retains the street encounter in both arrays and directive.

## RED evidence

`npm test -- --run src/engine/action-resolution.test.ts src/engine/action-narrative-context.test.ts`

- Exit 1: 2 failed, 58 passed.
- Resolver regression: at 17:05 after a five-minute explicit budget, `continuation` was `undefined` solely because the future boundary ID was `midnight`.
- Narrative regression: same-destination resume returned empty en-route arrays while its directive still required `street` and `detective-a`.

## GREEN verification

- Immediate two-file run after the fixes: 2 files, 60 tests passed.
- Fresh full Task 3 run:
  - `npm test -- --run src/engine/action-resolution.test.ts src/engine/scheduled-events.test.ts src/engine/action-narrative-context.test.ts`
  - Exit 0: 3 files, 74 tests passed, 0 failed.
- Scoped ESLint over all six Task 3 source/test files: exit 0, no output.
- Scoped strict TypeScript over the three production modules: exit 0, no output.

## Contract checks

- The future-midnight regression resumes the remaining 100 of 105 minutes, spends 1 then 13 stamina for the conserved total of 14, and grants the source only on completion.
- The existing actual-travel test now explicitly checks that `street` and `detective-a` remain required on the first travel resolution.
- The resume test checks that both en-route arrays are empty and the directive contains the no-encounter sentence, preventing replay.

Commit: pending root index GO; the resulting fresh commit will not be amended.

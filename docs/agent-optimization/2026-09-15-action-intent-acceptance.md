# 玩家意图一致性与合法轮回验收

## 判定规则

玩家选项的原文、起点、目标地点、行动种类与对象必须贯穿规划、程序执行和正文审查。程序决定耗时、资源、事件中断及结局优先级；现有编剧根据结算表现实际过程，现有审查要求纠正不一致。正常路径不新增模型调用。

按用户确认，体力或理智耗尽后合法回到08:00并进入下一轮属于完整轮回；它不等于覆盖到午夜的完整自然日。验收记录分别报告两者，不通过自动选择休息制造自然日覆盖。

## 实现与本地验证

- `7e152f7`：选项保存程序校验的意图，重试、读档及预规划保留同一绑定；执行前拦截目标错位；现有正文审查收到原意图、原计划和实际执行的对照。资源耗尽重置具有独立验收类型。
- `f319172`：真实启动句“决定接下来去哪里”不再误判为已要求出行。1307项测试通过、4项真实测试跳过，lint与构建通过。
- `5e59859`：区分问询内容里的移动和问询完成后的玩家移动；1318项测试通过，lint与构建通过。随后用户明确允许同一回合的跨地点剧情接续，因此此版没有再启动真实验收。
- 本地测试不是完整真实轮回验收的替代。

## 用户确认的剧情接续规则

用户明确选择“跨地点也可直接接续，必须服务原目标并结算路程”。因此原行动应是必须落实的开始，不能把整个回合锁成原地对话。导演可以随后安排有依据的核查或调查；所有新增路程与工作先列入计划、再按程序结算。现有正文审查判断因果联系与实际落实情况，不增加常规模型调用。

实现同时保留显式玩家限制、原事实授权和中断优先级。逐段场景契约覆盖原地点与后续地点；抵达不等于已完成接待或调查，续作不得重播已完成的路程。新地点案件事实不会仅因抵达自动取得授权。

## 真实模型记录

使用 `gemini-3.7-flash【果汁】`、standard模式、实际选项优先策略。各代码版本独立从08:00启动，不拼接跨版本结果。

| 活动 | 已接受行动/尝试 | 请求数 | 结果 |
| --- | --- | --- | --- |
| intent7e152f7-g37juice | 1/3 | 13 | 启动输入两次被疑问目的地误判拦截；随后测试器后备输入推进到08:55。主动停止修复，未完成轮回。文件中的临时turn-cap不是自然结束证据。 |
| intentf319172-g37juice | 1/2 | 18 | 启动调查正常到08:25，可操作等待52.8秒。下一步的候选选项“追问文穗早晨的具体走向与时间”被误判为出行，同时记录上游503，活动以provider-proxy-error停止。未完成轮回。 |
| followd5dabe2-g37juice | 4/7 | 73 | 同一代码、同一模型恢复一次后到12:20；原地调查、学校、便利店、商住楼均按选择执行。初次结构化审查缺字段及后续503导致停止。4次成功行动可操作等待合计206.3秒；未完成轮回。 |

上述失败活动保留于 `.codex-test-tmp/day-evaluation/`，未改写成通过。第一步正文仍有“屋里已经没有别的线索”等过度概括，不能据此声称故事质量全面通过。

`d5dabe2` 的逐行动记录另存于 [2026-09-15-followd5dabe2-evidence.json](2026-09-15-followd5dabe2-evidence.json)。该版本1346项测试、lint、构建与独立代码审查通过，但真实正文仍暴露审核漏项：

- 第2次尝试有翻书、衣柜和抽屉的具体过程，不能只因概括时间就判不足；“早有准备出门”的结论仍偏强。
- 第4、5次尝试回应原问题，但主要用“又核对几句”“翻来覆去确认”支撑长问询，过程充分性不足。
- 第6次尝试选择“问问文穗有没有回复她”，正文变成失联担忧、安抚及安排，没有明确提出并回应原问题。地点和人物正确不代表原请求完成。
- 第7次修复出现“目前我这边也还没有新的回音”，被事实审查拒绝。不能为了补回答而编造是否收到回复；明确拒答或回避也可以是合法的当下回应，但须让问题和未获答案对玩家可见。

后续修复复用同一次正文审查，要求结构化报告原问题回应、实际工作段过程与续展因果。合法报告判定剧情失败时进入编剧修复；缺字段、假引文或段ID错误先走现有一次审查报告纠正，重复无效保留待审稿，不无谓重写正文。历史记录不追溯改判。

同时修正未报价普通选项的默认深度：解析器的normal兜底不再视作玩家明确承诺。导演可以把未指定深度的问询安排成short，再接续适当调查；显式深度、程序菜单报价及续作进度继续受原规则约束，25/55/105分钟价格表未改。

## 当前结论

真实完整轮回验收仍待后续固定版本记录；当前不能据本地检查宣布完成或发布。

# 多真相、因果链与前三次重置复测

## 本次范围

按用户确认的互斥多真相设计统一世界规则：共同材料不提前证明当天生还、罪行或客观时间倒流；16:00事件是待核身份的初步通报。每条主路线补入可独立核对的中间因果证据，CULT/PSYCH保留基础路线的行为、死亡与责任。最终解答和普通结局要求第5日及完整证据链。

第一次、第二次、第三次重置分别演出“熟悉不等于知道去向”“文穗自己的意愿”“实际行动结果与时间、身份缺口”，进入现有播放器与消息存档。正常日终继续已选版本，结局后新版本不沿用上一版本的排他答案。

独立审查另外发现并修复了开局观察被写成客观生还、切换存档时旧待重置状态影响新存档，以及旧结局摘要借新开场进入下一版本上下文的问题。每日嫌疑上限保留为数值约束，合法新材料不应被预算强制转场阻断。

当前规则见 [story-world-rules.md](../story-world-rules.md)。原有事实和结局ID保留；旧存档缺少新增因果材料时，需要补调查，不能沿用孤立confirmation绕过证据门。

## 确定性验证

- 全量Vitest：1622通过、0失败、5跳过（包括默认关闭的真实付费探针）。报告：`.codex-test-tmp/story-complete-suite.json`。
- `npm run lint`、`npm run build` 均成功，生产构建完成；`git diff --check` 无错误。
- 嫌疑预算专项覆盖标准/严格编排中的新因果材料、旧事实表达、显式剧情转场和数值上限；兼容模式另由下述真实探针检查。

针对新版本开场摘要的测试先复现失败，再验证普通日终仍保留已执行行动后果、结局后的新开场与Director/Writer实际记忆输入不含旧答案；原始历史仍保留。

五条主线和两个解释层均有从空知识开始、遍历真实事实门可用材料的可达性测试，并校验缺失中间证据不能终局。这证明事实图与程序门相互可达，不等同于真实模型完整通关。

## 真实模型小样本

待填写结果。使用显式开启的 `scripts/live-action-authority.test.tsx`，在第3日16:00初报待送达的受控状态，分别运行standard与legacy，检查真实生成、审查、已接受正文和状态事务。每种模式一次，仅是局部冒烟验证。

数据库使用测试替身，因此不证明浏览器重载后的持久化；本次也不声称完整日程通关或模型语义错误已经消除。

## 发布

待填写提交、部署和生产资源核对结果。

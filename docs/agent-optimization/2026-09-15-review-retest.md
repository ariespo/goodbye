# 修复后的真实审查复测

## 结论

固定运行时代码 `bf23969`，使用原 7 个失败候选，保留正文、实际行动与权限边界。经用户允许，使用替代路由 `gemini-3.7-flash【神秘】` 完成审查及两稿重试：**6 稿最终获准，1 稿仍因报告错误失败**。其中 3 稿虽获准，人工复核仍发现语义漏审，不能据此宣称全部问题已经解决。

这次只运行正文审查，没有编剧重写和游戏状态提交，也没有重跑完整自然日。不同路由、固定失败样本及额外重试使其不能直接与原活动的 7/19 失败率比较。测试器的绿色只代表结果采集完成，并不代表所有候选获准。

## 逐稿结果

| 原尝试 | 主批次 | 单独重试 | 核验结论 |
| --- | --- | --- | --- |
| 6 | 获准 | — | 行动行号准确；门卫复述 F002 权限保留；不再把继续核对当作听见的证明，披露数组为空。 |
| 7 | 网络/401 中断 | 报告纠正后仍失败 | 初审把“摇了摇头”抄成“摇摇头”，不是原文引文；纠正响应在 assertions 数组中插入 `Wildcard`，触发 JSON SyntaxError，未获准。 |
| 8 | 获准 | — | 结构与行动引用正确；复合断言只引用 F001，未覆盖陈慧慧反应及灯织提醒；以清晨联系失败推导下午仍无回复，存在时间外推。相关经历存在原历史上下文，但未形成逐项引用。 |
| 10 | 两次网络失败后返回报告，听众纠正被本次 HTTP 上限阻断 | 一次报告纠正后获准 | 初审把“门卫隔着窗口给出了回答”当作玩家听见的证明；纠正后删除披露，保留 F002 引用及门卫发言权限，未修改正文。 |
| 13 | 获准 | — | 正确处理零分钟死亡消息事件，完整行动报告全部不适用；没有要求补问询，也没有让灯织凭“怎么了”获知电话内容。 |
| 16 | 获准 | — | 行动和连续性报告正常；原文“肯定会有正式的身份确认和询问”被审查命题弱化成“推测”，标成 hypothesis、无引用，遗漏了原文的确定性。 |
| 18 | 获准 | — | 正确审查5分钟旅行、365分钟等待及午夜中断，未要求补满阶段或提前重置；但“手机始终没有再次响起”仅引用未完成阶段无完整结果，来源不能证明无来电。 |

主批次 7 稿共 11 次 HTTP 尝试；针对 7、10 的单独重试共 5 次。两个进程都先尝试完整 schema，再按当前网关能力回退；这些数字包含格式能力探测及网络失败，不等于模型成功生成次数。7、10 的重试均使用原有一次报告纠正机会，没有增加纠正上限。已获准的其他5稿未进行报告纠正。

## 上游可用性与比较边界

- 原配置 `gemini-3.7-flash【果汁】` 返回过预付额度耗尽、429 和503；等待超过5分钟后，小请求仍返回429，提示上游项目月度额度耗尽。该信息来自代理返回，不能据此认定用户本人的充值账户余额。
- `deepseek-flash【果汁】` 的7个完整审查请求均遇上游400，但256输出 token 的小请求可以正常返回。具体参数或上游兼容原因尚未定位，因此没有把这组记作审查失败，也没有调整线上输出预算。
- `gemini-3.7-flash【神秘】` 小请求及完整审查均可返回；本报告的6/7来自该路由。`gemini-3.5-flash【果汁】` 的小请求也遇月度额度限制。
- 第18稿沿用旧 WriterPacket：原 Director brief 无法完整重建执行后计划，因此没有额外添加发言权限。其余六稿只添加当前程序由原授权上下文投影的 knownFactSpeakers；这仍不是完整回合的重新授权演算。
- 仅测试器增加原始响应留存、显式换模型与单稿重试选项。运行时源代码仍为 `bf23969`，本次没有用新提示词重测后替换统计。

## 下一步需要解决的问题

1. 断言引用仍需模型抄写完整原句；本次行动行号修复有效，但没有覆盖 assertionAudit 的引文错字。应把断言定位也交给程序回填，同时保留逐命题语义判断。
2. 审查命题必须保留原文的确定性、否定和时间范围，不能把“肯定”改成“推测”再免除来源要求。
3. 复合断言应逐项核对来源；已接受历史的适当来源投影需要补齐，同时禁止旧时间点的状态证明持续至现在，或用“无完整结果”证明期间无事件。

完整结果与相关断言保存在 [复测证据](2026-09-15-review-retest-evidence.json)。上述三项是复测发现的残留，没有在本次报告中标记为已修复。
